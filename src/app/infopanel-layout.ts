import { Component } from '@angular/core';

@Component({
  selector: 'app-purchase-order-table',
  standalone: true,
  templateUrl: './infopanel-layout.html',
  styleUrls: ['./infopanel-layout.scss']
})
export class PurchaseOrderTableComponent {
  // NOTE: In real implementation, values should be passed as @Input() from parent
  // For this static demo, values are hardcoded to match the design.
}

